<% content_for :email_content do
	case @user.signup_reason
		when "register_event"
			render('user_mailer/confirm_register_event')
		else
			render('user_mailer/confirm_default')
		end
end %>
<%= render(:template => 'user_mailer/header')%>