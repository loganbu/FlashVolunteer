<%

    user ||= current_user
    image_size ||= :thumb
    actions ||= :none
%>
<div class="row-fluid">
    <div class = "span2">
        <%= photo_link(user.avatar, image_size, user_url(current_user)) %>
    </div>
    <div class="span10">
        <p><%= user.name %></p>
    </div>
</div>